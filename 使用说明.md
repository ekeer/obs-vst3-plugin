# obs-vst3-plugin 使用说明

## 安装

### Windows

1. 下载 `obs-vst3-plugin-windows-x64.zip`
2. 解压，你会得到：
   - `obs-vst3.dll`
   - `obs-vst3.pdb`（调试符号，可留可不留）
3. 复制到 OBS Studio 插件目录：
   ```
   C:\Program Files\obs-studio\obs-plugins\64bit\
   ```
   或者你的用户数据目录：
   ```
   %APPDATA%\obs-studio\plugins\
   ```
4. 重启 OBS Studio

### Linux

1. 下载 `obs-vst3-plugin-linux-x64.zip`
2. 解压得到 `obs-vst3.so`
3. 复制到 OBS Studio 插件目录（通常是）：
   ```
   /usr/lib/obs-plugins/
   ```
   或
   ```
   ~/.config/obs-studio/plugins/
   ```
4. 重启 OBS Studio

## 基本用法

### 添加 VST3 效果器

1. 在 OBS 中，选择任意音频源（麦克风、音频输出捕获、媒体源等）
2. 点击 **滤镜** 按钮
3. 点击 **+** → 选择 **VST3 滤镜**
4. 点击 **浏览** 选择你的 `.vst3` 插件文件
5. 调节参数即可

### 侧链（Sidechain）功能

部分 VST3 插件支持侧链输入（如压缩器的外部触发信号）：

1. 添加 VST3 滤镜后，在滤镜属性中启用侧链
2. 选择侧链音频源（另一个音频源作为触发信号）
3. 插件会自动处理侧链音频路由

### 链式/图路由（Graph/Chain）

此插件支持在同一滤波器上串联多个 VST3 插件：

1. 添加 VST3 滤镜后，在属性面板中点击 **Add VST3 Plugin...**
2. 插件会按顺序串联处理音频
3. 可以通过 **Remove Selected** 移除不需要的节点
4. 使用 **Save Chain Config...** / **Load Chain Config...** 保存和加载插件链配置

> **注意**：图路由功能为 v0.1.0 的新特性，具体交互界面正在完善中。

## 常见问题

### Q: 插件加载失败？
- 确保你的 VST3 插件是 64 位的（OBS Studio 是 64 位应用）
- 检查插件文件是否为 `.vst3` 格式（不是 `.dll` 也不是 `.vst2`）
- 检查 OBS Studio 日志文件（帮助 → 日志文件 → 查看当前日志）

### Q: 音频无输出？
- 确认滤镜已启用（勾选框）
- 确认 VST3 插件本身有输出（有些插件需要旁通模式才能听到干声）
- 检查插件属性中的输入/输出配置

### Q: 延迟过高？
- VST3 插件会增加音频处理延迟
- 一些混响、母带插件延迟较高，可尝试在插件内部调整缓冲大小

### Q: 如何获取 VST3 插件？
- 免费 VST3 插件可从以下网站获取：
  - [Plugin Boutique](https://www.pluginboutique.com/)（免费/付费）
  - [KVR Audio](https://www.kvraudio.com/)（免费/付费）
  - [ValhallaDSP](https://valhalladsp.com/)（免费 Freq Echo / 付费）
  - [TDR Labs](https://www.tokyodawn.net/tokyo-dawn-labs/)（免费）
  - [MeldaProduction Free Bundle](https://www.meldaproduction.com/MFreeFXBundle)（免费）

## 构建说明

此插件构建自 [pkviet/obs-studio](https://github.com/pkviet/obs-studio/tree/vst3) 的 `vst3` 分支。
如需自行编译，请参考 CI 配置：`.github/workflows/build.yml`。
